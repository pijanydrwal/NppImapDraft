#ifndef IMAP_CLIENT_H\n#define IMAP_CLIENT_H\n\n#include <string>\n#include <vector>\n\nclass ImapClient {\npublic:\n    ImapClient(const std::string &server, int port);\n    bool login(const std::string &username, const std::string &password);\n    std::vector<std::string> listMailboxes();\n    bool selectMailbox(const std::string &mailbox);\n    std::vector<std::string> fetchMessages(int limit);\n    void logout();\n\nprivate:\n    std::string server;\n    int port;\n    std::string username;\n    std::string password;\n};\n\n#endif // IMAP_CLIENT_H